---
- name: Pull docker image of expensetracker app
  hosts: all
  tasks:
    - name: Pull Expense Tracker app docker image
      docker_image:
        name: ssvapp/expense-tracker
        source: pull
